# Local data directory

FloatChat may persist the local documentation vector store here. Demo data is generated in memory. Uploaded NetCDF and generated Parquet files should be treated as local data and are excluded from Git.
